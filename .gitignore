# local config
.env

# python
.venv/
__pycache__/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage

# generated source system (deterministic: regenerate with `refarch generate`)
source_system/generated/

# the warehouse (rebuild it with `refarch run`)
warehouse/

# dlt / dbt run artefacts
.dlt/pipelines/
.dlt/secrets.toml
dbt/target/
dbt/.user.yml  # dbt writes a local anonymous-user id here
dbt/dbt_packages/
dbt/logs/
logs/

# Local agent config — not part of the published example
.claude/
