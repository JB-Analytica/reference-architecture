# secrets and local config
.env
*.json.key
service-account*.json
.sa.json

# python
.venv/
__pycache__/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage

# generated source system (deterministic: regenerate with `refarch generate`)
source_system/generated/

# dlt / dbt run artefacts
.dlt/pipelines/
.dlt/secrets.toml
dbt/target/
dbt/dbt_packages/
dbt/logs/
logs/

# Lightdash CLI download bookkeeping (local timestamps, not content)
lightdash/.lightdash-metadata.json

# Local agent config — not part of the published example
.claude/
